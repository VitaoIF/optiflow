services:
  postgres:
    image: postgres:16
    container_name: optiflow-db
    environment:
      POSTGRES_DB: optiflow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"